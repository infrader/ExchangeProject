# 🚀 High-Performance Market Data Aggregator (ExchangeProject)

**C++ | Multithreading | Lock-free Design | Real-time Data Processing**

[![C++ Standard](https://img.shields.io/badge/C++-17/20-blue.svg)](https://en.cppreference.com/w/)
[![License](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)

Этот проект представляет собой высокопроизводительное ядро для агрегации и кэширования биржевых данных, спроектированное с акцентом на минимальные задержки (low-latency) и потокобезопасность для среды с одним писателем и множеством читателей.

## ✨ Ключевые особенности

*   **🔄 Паттерн Single-Writer/Many-Readers**: Специально разработан для сценариев, где данные обновляются реже, чем читаются.
*   **⚡ Lock-free обновление данных**: Использование атомарных операций (`std::atomic`) и барьеров памяти (`std::memory_order`) для безопасного обновления кэша без блокировок читателей.
*   **🧵 Потокобезопасная архитектура**: Четкое разделение на поток-писатель (обновляет данные) и потоки-читатели (получают данные).
*   **🛡️ Надежная обработка исключений**: Механизм передачи исключений из фонового потока в основной с использованием `std::exception_ptr`.
*   **🎯 Минимальные задержки**: Использование семантики перемещения (`std::move`) для мгновенного переключения данных и активного ожидания с микросекундными паузами.

## 🏗️ Архитектура и дизайн
*   **Класс `Exchange` (Абстрактный)**: Ядро системы. Определяет цикл обновления, механизм синхронизации и предоставляет интерфейс для чтения данных. Поддерживает добавление новых бирж через переопределение виртуального метода `parse()`.
*   **Синхронизация**: Реализована через комбинацию `std::atomic<bool>` флага и барьеров памяти (`std::memory_order_acquire`/`release`), гарантируя видимость обновленных данных для всех читателей.
*   **Логгирование**: Модульный логгер с поддержкой различных уровней сообщений (TRACE, DEBUG, INFO, WARN, CRITICAL).

## 🛠️ Технологический стек

*   **Язык:** C++17/20
*   **Многопоточность:** `std::thread`, `std::atomic`, `std::condition_variable`, `std::memory_order`
*   **Сеть:** [cpr](https://github.com/libcpr/cpr) (HTTP-клиент для запросов к API)
*   **Время:** `<chrono>`
*   **Структуры данных:** STL (`std::unordered_map`, `std::deque`, `std::string`)
*   **Логгирование:** Собственная потоконебезопасная реализация (в процессе доработки)

## 📦 Установка и сборка

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/infrader/ExchangeProject.git
    cd ExchangeProject
    ```

2.  **Установите зависимости:**
    *   **cpr:** Следуйте инструкциям на [официальной странице](https://github.com/libcpr/cpr).
    *   **CMake** (версии 3.14 и выше).

3.  **Соберите проект:**
    ```bash
    mkdir build && cd build
    cmake ..
    cmake --build .
    ```

## 🚦 Использование

Основной класс `Exchange` является абстрактным. Для его использования необходимо:
1.  Создать производный класс для конкретной биржи.
2.  Реализовать метод `parse()`, который преобразует сырой ответ API в `std::unordered_map<std::string, TokenInfo>`.
3.  Запустить поток обновления с помощью `spin_upload_start()`.
4.  Получать актуальные данные через `get_data()` из любого количества потоков.

```cpp
// Пример псевдокода
class BinanceExchange : public Exchange {
    std::unordered_map<std::string, TokenInfo> parse(cpr::Response r) override {
        // Парсинг JSON-ответа от Binance API
        // Возврат map с данными
    }
};

int main() {
    BinanceExchange exchange;
    exchange.set_api("https://api.binance.com/api/v3/ticker/bookTicker");
    std::thread updater(&BinanceExchange::spin_upload_start, &exchange);

    // В рабочем потоке (читателе)
    auto& current_data = exchange.get_data(); // Безопасное получение ссылки на данные
    // ... используем current_data ...

    exchange.spin_upload_stop();
    updater.join();
    return 0;
}
