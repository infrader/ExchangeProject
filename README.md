# High-Performance Market Data Aggregator (C++ Multithreading)

**Реализация потокобезопасного ядра для агрегации биржевых данных в реальном времени с минимальными задержками.**

[![C++17/20](https://img.shields.io/badge/C++-17/20-blue)](https://en.cppreference.com/)
[![Multithreading](https://img.shields.io/badge/Многопоточность-Advanced-green)]()

## 🎯 Решенные технические задачи

### 1. **Архитектура Single-Writer/Many-Readers**
- Спроектировал и реализовал паттерн с одним потоком-писателем и неограниченным числом читателей
- Гарантировал консистентность данных при конкурентном доступе

### 2. **Lock-Free Синхронизация через Atomic Operations**
- Применил `std::atomic<bool>` с барьерами памяти (`std::memory_order_acquire/release`)
- Использовал `std::atomic_signal_fence` для гарантии порядка операций
- Минимизировал contention: читатели не блокируют друг друга

### 3. **Межпоточная обработка исключений**
- Реализовал механизм безопасной передачи исключений между потоками через `std::exception_ptr`
- Обеспечил стабильность фонового потока при ошибках парсинга

### 4. **Оптимизация производительности**
- Использование семантики перемещения (`std::move`) для крупных структур данных
- Активное ожидание с микросекундными паузами вместо блокировок
- Минимальное копирование данных при обновлении кэша

## 🏗️ Архитектура системы
[Поток Writer] → HTTP API → Парсинг JSON → Atomic Swap → [Потокобезопасный Кэш]
↓
[Неограниченно потоков Readers]
**Ключевые компоненты:**
- `Exchange` (абстрактный базовый класс) — ядро системы, управление циклом обновления
- `KuCoin` — конкретная реализация для биржи KuCoin с парсингом JSON
- Механизм исключений с `std::deque<std::exception_ptr>` и атомарным флагом
- Потокобезопасный логгер с поддержкой уровней логирования

## 📈 Метрики и показатели

- **Обновление данных:** каждые 3 секунды
- **Задержка обновления кэша:** < 1 мс (atomic swap)
- **Поддерживаемый объем:** 2000+ торговых пар
- **Потребление памяти:** константное, независимо от числа читателей

## 🚀 Технологический стек

```yaml
Язык: C++17/20
Многопоточность: std::thread, std::atomic, std::memory_order
Сеть: cpr (HTTP-клиент), REST API
Данные: nlohmann/json, std::unordered_map
Синхронизация: атомарные операции, condition variables
Утилиты: std::format, <chrono>, RAII

# Клонирование и сборка
git clone https://github.com/infrader/ExchangeProject.git
cd ExchangeProject
mkdir build && cd build
cmake .. -DCMAKE_BUILD_TYPE=Release
cmake --build .

# Запуск примера
./bin/exchange_example
